# Generated model artifacts

This directory is populated locally by `python scripts/train_model.py`.

The generated Random Forest model (`flood_risk_model.joblib`) and its metrics, feature-importance, and dataset-statistics JSON files are intentionally ignored by Git. Recreate them after downloading the dataset described in [`../data/README.md`](../data/README.md).
